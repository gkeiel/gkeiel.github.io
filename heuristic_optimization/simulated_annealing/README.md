# simulated_annealing

A simple simulated_annealing algorithm for finding the minimum of a function. Objective function must be provided. Although a fixed temperature decay rate is used, its method can be easily modified.